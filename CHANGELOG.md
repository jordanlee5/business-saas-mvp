# 版本说明

本文件记录正式版本收口内容。版本号用于说明可复核的功能边界，不代表当前系统已经完成生产部署加固。

## v0.5.0-m6-3（2026-09-21）

- 新增供应商结算原子确认服务，只允许启用中的超级管理员把证据完整的 `PENDING_CONFIRMATION` 批次推进到 `CONFIRMED`；
- 确认前重新锁定并核验批次、全部明细、来源订单项、订单完成及未退款事实、生成审计与合计，确认状态、确认人、确认时间和 `mall_supplier_settlement_confirm` 审计同事务提交；
- 精确重放与并发确认只保留一套确认事实和审计记录；时间倒置、来源退款、快照漂移、合计或审计异常时失败关闭并整体回滚；
- 订单任一明细进入供应商结算后，整单自动退款失败关闭，避免待确认批次失效或已确认结算与退款事实冲突；异常场景留待后续人工冲销流程处理；
- 本切片继续复用 `0011_supplier_settlement_foundation`，不新增迁移，不开放结算页面、HTTP API、Excel、支付、撤销或冲销，现金返现链路保持不变。

## v0.5.0-m6-2（2026-09-21）

- 新增供应商结算批次原子生成服务，只选择指定供应商在左闭右开区间内仍为 `COMPLETED`、无退款事实且从未进入结算的订单项；
- 生成时固化供应商、订单、商品、SKU、数量和人民币成本快照，重算订单数、明细数、商品数量及成本总额，并以 `PENDING_CONFIRMATION` 状态保存；
- 只有启用中的超级管理员和运营管理员可以生成；批次、明细与 `mall_supplier_settlement_generate` 管理员审计在同一事务提交，任一证据异常时整体回滚；
- 同一订单项依靠查询排除、供应商行锁和全局唯一约束避免跨批次重复结算；重复或并发生成没有新增合格订单项时失败关闭；
- 本切片复用 `0011_supplier_settlement_foundation`，不新增迁移，不开放结算确认、撤销、支付、Excel、HTTP API 或后台页面，也不改变订单退款与现金返现链路。

## v0.5.0-m6-1（2026-09-18）

- 新增供应商结算状态领域值，以及 `supplier_settlement_batches`、`supplier_settlement_items` 两张表，保存供应商、结算区间、订单项人民币成本和生成/确认操作者快照；
- 结算批次只允许待确认或已确认，确认状态必须具有完整操作者与时间证据；每个订单项最多进入一个结算明细，数量与成本运算由数据库约束失败关闭；
- 新增 `0011_supplier_settlement_foundation` 线性迁移，只创建空表而不改写现有订单、积分、库存或现金返现数据；存在结算批次或明细时拒绝降级丢失；
- 结构就绪检查升级为 28 张必需应用表，SQLite 副本升级演练新增已审计 `0010_order_refund_recovery` 源库支持；本切片不开放结算生成、确认、导出或页面。

## v0.5.0-m5-6（2026-09-17）

- 新增 `COMPLETED → REFUNDED` 内部整单退款服务，在一个幂等事务中把积分退回订单原消费批次、把库存退回原 SKU，并保存退款原因、时间和管理员审计；
- 只有启用中的超级管理员和运营管理员可以退款；相同原因精确重放及并发请求只保留一套积分 `REFUND`、库存 `RETURN` 与 `mall_order_refund` 证据；
- 退款前验证订单快照、确认履约、发货、完成、积分消费及库存出库完整证据；原积分批次到期、状态异常、余额漂移或证据被篡改时失败关闭并整体回滚；
- 新增 `0010_order_refund_recovery` 线性迁移，为订单增加退款原因、退款时间及状态完整性约束；已有无事实证据的 `REFUNDED` 数据拒绝自动升级，存在退款证据时拒绝降级丢失；
- 升级副本演练扩展为接受已审计的 `0009_order_shipping_completion` 源库；本切片不开放订单 API 或页面，不实现部分退款、异常人工恢复或供应商结算冲销。

## v0.5.0-m5-5（2026-09-17）

- 新增 `FULFILLING → SHIPPED → COMPLETED` 内部订单生命周期服务，按已确认业务规则手工记录物流公司、运单号、操作人、发货时间及完成时间；
- 只有启用中的超级管理员和运营管理员可以首次发货或确认完成；精确重放与并发请求只保留一套状态和管理员审计证据；
- 操作前重算订单快照合计，并验证原履约、积分消费、库存出库及物流审计证据；状态、时间或证据异常时失败关闭并整体回滚；
- 新增 `0009_order_shipping_completion` 线性迁移，为订单增加手工物流与完成证据字段、状态约束、物流唯一约束和查询索引；存在旧的 `SHIPPED`/`COMPLETED` 数据时拒绝无证据自动升级；
- 升级副本演练扩展为接受已审计的 `0008_order_reservation` 源库；本切片不开放订单 API 或页面，不实现退款、退库、异常恢复或供应商结算。

## v0.5.0-m5-4（2026-09-16）

- 新增 `CREATED → FULFILLING` 内部订单确认履约服务，在同一幂等事务中把原积分预占转为 `CONSUME`、原 SKU 库存预占转为 `OUTBOUND`；
- 只有启用中的超级管理员和运营管理员可以确认履约；重复或并发操作只保留一套积分消费、库存出库和管理员审计证据；
- 原预占批次即使已越过到期时刻仍可闭合既有订单消费，但冻结批次失败关闭；订单合计、原预占、消费出库或余额流水异常时整笔回滚；
- 本切片不新增数据库迁移，继续使用 `0008_order_reservation`，不开放订单 API 或页面，也不实现发货、完成、退款、退库或供应商结算。

## v0.5.0-m5-3（2026-09-16）

- 新增 `CREATED → CANCELLED` 内部订单取消领域服务，在同一幂等事务中释放订单的 SKU 库存与原积分批次预占；
- 库存和积分分别追加不可变 `RELEASE` 流水，重复或并发取消不重复释放；订单合计、原预占、释放证据或余额流水异常时失败关闭；
- 取消不修改积分原到期时刻；已到期但仍被订单预占的批次先释放关联预占，再由既有到期任务按原时刻核销，不延长有效期；
- 本切片不新增数据库迁移，继续使用 `0008_order_reservation`，不开放取消 API 或页面，也不实现消费、发货、退款或供应商结算。

## v0.5.0-m5-2（2026-09-15）

- 确认供应商只结算 `COMPLETED` 且未退款订单的人民币成本快照；本切片不实现实际结算；
- 新增订单领域服务，在一个幂等事务内创建订单快照并完成 SKU 库存、积分批次 FEFO 原子预占；
- 新增 `0008_order_reservation` 线性迁移：订单增加幂等键，库存流水增加预占数量前后值、会员操作者和订单来源；
- 库存余额审计扩展为同时重算现存与预占数量；积分或库存不足、目录不可兑换或余额漂移时整笔订单失败关闭；
- 本切片不开放下单接口或页面，不实现取消、消费、发货、退款或供应商结算。

## v0.5.0-m5-1（2026-09-14）

- 新增 `0007_order_foundation` 线性迁移，建立订单主表、订单项快照表和订单积分批次分配表；
- 订单项固化商品、SKU、供应商、单位积分价、单位人民币成本、数量和行合计，目录后续变化不得回写历史快照；
- 结构就绪检查升级至 0007 和 26 张必需表，SQLite 副本升级演练支持审计后的 0006 源库；
- 本切片不开放下单接口或后台页面，不执行积分/库存写操作，不实现供应商结算，也不写入真实测试数据。

## v0.4.3 — 2026-09-08

### M4-6 库存管理后台（2026-09-14）

- 左侧“积分商城”新增独立“库存管理”入口，超级管理员和运营管理员可以进入；初审、复核、上传方及无效权限账号继续失败关闭；
- 新增 `/mall-inventory` SKU 库存概览，支持商品、SKU、供应商关键词筛选和正常、低库存、无库存状态筛选；未发生库存操作的 SKU 只读显示为零库存，不创建虚假余额行；
- 页面同时展示现存、预占、可售、预警阈值、余额版本、流水数量及账实一致性；发现余额无法由流水重算时明确标红、停止该 SKU 页面写入且不自动修复；
- 入库和人工调整表单继续调用 M4-5 受控领域服务，操作者只取服务端登录会话，页面生成一次性幂等请求号；重复提交不重复增加库存、流水或管理员日志；
- 新增按 SKU 筛选和分页的库存流水视图，展示稳定流水编号、类型、前后数量、余额版本、原因、操作者及 UTC+8 时间；流水不提供编辑或删除入口；
- 页面写入把余额、不可变流水和管理员日志放在同一请求事务；校验、权限、账实异常、数据库锁或唯一冲突均回滚并返回受控提示，未预期异常回滚后继续抛出；
- 本切片不新增数据库迁移、不预置真实目录或库存数据，也不实现商品/库存 Excel、订单预占/出库、退款或小程序商品接口，现金返现链路保持不变。

### M4-5 SKU 库存余额与流水基础（2026-09-11）

- 新增 `0006_inventory_foundation` 线性迁移及 `inventory_balances`、`inventory_movements` 两张空表，库存管理到 SKU，并预留后续订单使用的预占数量；
- 入库和库存调整统一通过领域服务追加流水，保存稳定流水编号、幂等键、变动前后数量、余额版本、原因、操作者和时间；库存余额不得被业务入口静默覆盖；
- 同一幂等请求安全重放且不重复增加库存或日志，参数或操作者冲突失败关闭；库存不能调整到低于已预占数量；
- 新增按顺序流水重算现存数量与余额版本的审计能力，缓存被篡改或流水断链时明确阻止；低库存与无库存状态按可售数量及 SKU 阈值计算；
- 入库和调整仅允许启用中的超级管理员、运营管理员调用，每次实际变化同步写入独立管理员操作日志；初审、复核、上传方及无效账号拒绝；
- 结构就绪检查扩展为 23 张必需表；副本演练新增真实 `0005_product_media` 源库预检，并证明原表结构和字段值不变后才在副本升级至 0006；
- 本切片不增加库存管理页面、Excel、订单预占、出库、退回、小程序商品接口或真实库存样例数据，现金返现链路保持不变。

### M4-4 商品媒体基础与后台维护（2026-09-10）

- 新增 `0005_product_media` 线性迁移和 `product_media` 表，保存商品主图、轮播图、详情图、排序、启用状态、替代文字及上传管理员；条件唯一索引保证每个商品最多一张主图；
- 商品图片独立保存在 `uploads/mall_products/<商品公开编号>/`，不复用宣传页素材目录；只接受实际内容匹配的 JPG、PNG、WebP，限制 5 MB、尺寸与像素总量，并统一转换为最长边不超过 2400 像素的 WebP；
- 商品目录后台新增图片预览、上传/主图替换、元数据编辑和删除；主图数据库提交成功后才清理旧文件，数据库失败时回收新文件；
- 新增商品媒体创建、更新、删除审计动作，超级管理员和运营管理员可操作，其他角色与无效账号继续失败关闭；
- 结构就绪检查扩展为 21 张必需表；副本升级演练新增真实 `0004_catalog_foundation` 源库预检，并证明旧表结构与字段值不变后才在副本升级至 0005；
- 本切片不改变既有商品上架条件，不实现库存、商品/库存 Excel、商城横幅、订单或小程序商品接口。

### M4-3 商品目录管理后台（2026-09-09）

- 左侧“积分商城”新增“商品目录”入口，超级管理员和运营管理员可以进入；初审、复核、上传方及无效权限账号继续失败关闭；
- 新增商品、分类和供应商三个页面分区，可从页面创建、编辑分类与供应商，创建草稿商品和 SKU，编辑商品资料、SKU 当前价格/成本/阈值/排序及启停状态；
- 商品上下架只调用 M4-2 专用状态服务，无有效 SKU 的商品不能上架，草稿不能直接下架，页面不能绕过分类、供应商和最后一个启用 SKU 的保护；
- 所有成功表单操作在同一请求事务提交目录变化和管理员日志；业务或权限失败整体回滚，数据库冲突使用受控提示，未预期异常回滚后继续抛出；
- 页面展示先构造只读目录快照，不在查询时修复或写入数据；库存尚未建立前不虚构现存、可售或低库存状态；
- 本切片不新增迁移、媒体、库存、商品/库存 Excel、删除、订单或小程序商品接口。

### M4-2 商品目录写服务

- 新增受权限与审计保护的商品分类、供应商、商品和 SKU 创建/编辑领域服务；全部写操作由调用方在同一事务提交或回滚；
- 商品与供应商稳定公开编号由服务端生成，SKU 编码创建后不可修改；创建商品固定进入草稿状态，不能由调用方绕过状态机直接上架；
- 上架要求分类启用，且至少存在一个启用、供应商有效的 SKU；草稿不能直接下架，重复上下架为无写入、无重复审计的安全操作；
- 已上架商品所在分类不能停用，已被上架商品在售 SKU 使用的供应商不能停用，已上架商品最后一个启用 SKU 不能停用；
- SKU 积分售价与人民币成本继续使用两位小数，分别执行正数与非负校验；分类 slug、排序、启用状态和唯一字段在服务入口失败关闭；
- 新增商品分类审计动作，并保持超级管理员、运营管理员可写，初审、复核、上传方、停用及未知账号拒绝；
- 本切片不新增迁移、页面、HTTP 写接口、媒体、库存余额/流水、Excel 导入导出、订单或小程序商品 API。

### M4-1 商品目录数据基础

- `OPEN-006` 确认为退款退回原消费积分批次，原批次在退款时已到期则进入人工处理，不自动延长或新建临时积分批次；
- `OPEN-007` 确认为第一版全部包邮，不收取现金或积分运费，后续变更不得回写历史订单；
- 新增 `0004_catalog_foundation` 线性迁移，建立 `product_categories`、`suppliers`、`products` 和 `product_skus` 四张空表；
- 商品状态限定为 `DRAFT`、`PUBLISHED`、`UNPUBLISHED`，上架必须保存发布时间，分类、商品和 SKU 排序值均不得为负；
- SKU 绑定商品与供应商，积分售价和人民币成本使用 `NUMERIC(18, 2)`；售价必须为正，成本及低库存阈值不得为负；
- 应用结构就绪检查扩展至 20 张必需表和商品目录关键字段；虚假 `0004` 标记或缺失目录结构时继续失败关闭；
- SQLite 副本升级演练新增 `0003_member_activation_security → head` 支持，并在复制前核验激活安全表及关键字段；
- PostgreSQL 离线迁移继续覆盖完整 metadata，并增加 SKU 价格、成本精度断言；
- 本切片不增加商品后台页面、写服务、媒体、库存、Excel 导入导出、订单或小程序商品 API；升级后目录表为空是预期结果。

## v0.4.2 — 2026-09-03

### M3-5 会员积分详情与 Excel 导出（2026-09-08）

- 新增管理员侧“会员积分”列表与详情页，支持会员状态和会员/来源业务关键词筛选，展示账户可用与预占积分、独立积分批次、到期时间及不可变流水；
- 页面来源客户姓名、手机号和车牌号统一脱敏，不显示微信 `openid/unionid`；查询全程只读，不触发到期任务、不冲刷待保存对象，也不静默修复余额；
- 详情按积分批次到期时间排序，已到时刻但尚未执行维护的批次明确标记“待到期处理”；账户和批次缓存与流水不一致时明确显示异常；
- 超级管理员和运营管理员可查看及导出，初审、复核、上传方、无效或未知级别账号失败关闭；导航入口与路由使用相同的独立细权限；
- 单会员 Excel 包含“会员汇总”“积分批次”“积分流水”三个工作表，不包含微信身份、激活凭据或幂等键，并对公式型文本进行转义；
- 每次成功生成 Excel 都在同一请求事务写入 `mall_member_points_export` 管理员操作日志，记录操作者、目标会员、批次/流水数量、账本一致性和生成时间；失败时回滚日志；
- 本轮不新增数据库迁移，不开放小程序会员积分 API，不实现预占、消费、退款、商品或订单。

### M3-4 超级管理员积分调整领域服务（2026-09-07）

- 新增只追加的人工积分纠错服务，仅允许当前启用中的超级管理员执行；运营、初审、复核、上传方、停用账号和不存在账号全部失败关闭；
- 正向调整只能恢复既有批次至原始授予上限，负向调整只能扣减可用积分；不修改原始授予额、预占余额、到期时间或来源业务，不借人工调整提前实现奖励、消费或退款规则；
- 每次成功调整同时新增 `ADJUST` 积分流水和 `mall_points_adjust` 管理员操作日志，流水反向关联日志；原因、操作者、时间、批次及请求号均可追溯；
- 请求号具有全局幂等性：完全相同的请求可安全重放，不重复记账或增加账户版本；内容、操作者、批次或审计日志不一致时拒绝继续；
- 写入前后均按不可变流水核对账户与全部批次缓存，发现任何漂移立即失败；调整、日志、流水、批次余额、账户余额及版本在同一事务整体提交或回滚；
- SQLite 独立入口以 `BEGIN IMMEDIATE` 串行化写操作，取得锁后读取最新余额；并发不同请求不会丢失更新，并发重复请求只创建一笔调整；
- 本轮不新增迁移、命令行、后台页面或 API。未来页面必须从已认证服务端会话取得操作者身份，不能接受客户端指定管理员编号。

### M3-3 积分到期处理与即将到期查询（2026-09-07）

- 新增只读的已到期批次与未来指定天数到期批次查询，提供账户过滤、ID 游标分页、可用/预占余额和阻止原因；
- 按 UTC+8 判定 `expires_at <= 当前时刻`，到期仅扣除该批次剩余可用积分，追加唯一 `EXPIRE` 流水并同步账户、批次和版本；已用尽的零余额批次只关闭状态，不生成零金额流水；
- 重放不会重复扣分；冻结、预占余额、缓存漂移、未入账及异常状态失败关闭；来源业务保持已激活，不退回待激活；
- 新增默认只读的 `python -m app.points_expiry_task` 命令，显式 `--apply` 才写入；每页整体提交或回滚，有阻止项时整页不写入；
- SQLite 执行入口先取得写事务；积分入账与到期处理统一先账户、后批次的锁顺序，并读取锁取得后的当前余额，避免旧 ORM 缓存干扰；
- 本轮不新增迁移、API、后台页面或自动计划任务；真实 PostgreSQL 并发验证仍需独立测试环境，SQLite 并发测试不替代该门槛。

### M3-2 积分账本一致性基础（2026-09-07）

- 新增积分流水领域服务，首笔 `GRANT` 入账统一通过只追加入口写入，并在同一事务同步积分批次与会员账户余额缓存；
- 相同内容的幂等请求重复执行不会二次入账，同一幂等键携带不同来源信息时失败关闭；
- 新增按不可变流水重算单个积分批次和会员账户可用/预占余额的只读能力；
- 新增账户级一致性审计，同时比较账户缓存、全部批次缓存与流水重算结果，发现漂移时只报告或拒绝后续写入，不静默修复；
- 现有会员激活流程改为复用统一首笔入账服务，仍保持单事务回滚、两位小数精度和多积分批次汇总规则；
- 本切片不新增数据库迁移，不开放积分 API 或后台页面，也不提前实现到期、人工调整、FEFO、消费和退款。

### M3-1 会员激活与首笔积分入账基础

- `OPEN-004` 确认为一期使用一次性激活码，并在同一领域接口中预留后续短信验证码方式；短信通道未接入前失败关闭；
- 新增 `0003_member_activation_security` 迁移和 `member_activation_credentials` 表，一条商城业务只有一个当前凭据；
- 一次性码使用排除混淆字符的随机值，只在签发时返回一次；数据库仅保存随机盐、PBKDF2-SHA256 摘要、算法参数和重签版本，不保存明文；
- 凭据记录错误次数并在达到上限后锁定，支持已使用、已过期和已撤销状态；预期激活失败统一返回不可枚举文案；
- 只有已承接、未过领取截止日且仍待激活的商城业务可以签发凭据；现金返现业务保持隔离；
- 激活成功在同一事务创建或复用微信会员和积分账户，生成独立积分批次、首笔 `GRANT` 流水，并更新业务与凭据状态；
- 同一业务的积分批次唯一约束及流水幂等键共同阻止重复入账；同一微信会员可以领取多条业务形成的独立积分批次；
- 升级副本演练扩展为同时支持 `0001 → head` 与现网 `0002 → head`，并继续逐表校验原字段和数据指纹不变；
- 公网激活 API、微信登录态、请求级限流、短信发送、商品和订单均未在本切片开放。

## v0.4.1 — 2026-09-03

### M2 上传渠道拆分

- Excel 上传新增现金返现与商城积分渠道选择，商城批次强制填写领取截止日；
- 批次与所属业务记录同步保存渠道快照，商城业务初始化为待激活；
- 批次、业务列表、详情、筛选和 Excel 导出支持渠道及领取信息；
- 旧凭证匹配候选、凭证批次选择和经营统计显式限定现金返现业务；
- 管理员只能在批次待承接阶段纠正错选渠道，纠正同步更新所属业务并记录操作日志；
- 现金返现继续强制填写银行卡号，商城积分允许银行卡号留空；姓名、手机号、车牌号和积分金额仍为两种渠道的必填字段；
- Excel 改为整批原子校验：任意一行不合格时整份清单拒绝导入，不生成批次、业务数据或通知；历史零成功批次不再出现在常规批次列表；
- 承接或拒绝后可填写原因撤销并恢复为待承接；已产生凭证审核、积分权益或商城领取处理的批次禁止撤销，所有成功撤销均写入管理员操作日志；
- 业务数据与上传记录页面压缩批次字段和操作区，长文件名按“前 2 字 + `....` + 末 4 字”展示，完整文件名保留在悬停提示中；
- 现金返现仍为默认渠道，历史数据和现有操作流程保持不变；
- 未新增数据库迁移，继续使用 `0002_mall_core_foundation`。

## v0.3.0 — 2026-08-27

### 版本定位

正式收口当前业务数据管理 MVP，并把积分商城确立为下一条独立开发路线。

收口前稳定代码基线：`883529789b4977dc04c86874ccfcded8df336f57`（`8835297 add allocation amount anomaly alerts and filtering`）。

### 已纳入本版本的现有能力

- 多管理员账号、分级权限、停用恢复、首次登录改密和操作日志；
- 业务上传、批次承接隔离、公开业务单号、列表详情筛选与 Excel 导出；
- 凭证 OCR、匹配、初审与二级复核、审核人隔离和跨页批量审核；
- 本次核销金额、业务与凭证双重额度限制、已付清关闭审核池；
- 核销完成度、剩余金额、金额异常提示、筛选、统计与下钻；
- 经营数据、上传方结算、通过凭证下载及 UTC+8 时间统一；
- 工作台堆叠通知卡片；
- 可配置保险公司宣传页、图片、草稿、发布、下线、预览和公开路由。

### 本次收口变更

- 将 README 版本从 v0.1.0 更新为 v0.3.0；
- 更新功能清单和现金返现核销术语；
- 记录稳定 commit、全量测试基线和现有数据库迁移边界；
- 写明 SQLite、本地上传目录与生产部署的能力边界；
- 新增商城业务规则决策记录，区分已确认约束与待确认参数。

### 明确未变更

- 未修改 Python 业务代码、数据模型、路由或页面模板；
- 未新增商城数据库表、API 或页面；
- 未执行数据库迁移；
- 未改变现金返现凭证上传、匹配、审核、核销、统计、结算或 Excel 行为；
- 未重新扩展已经收口的历史核销异常审计。

### 回归基线

- `python -m compileall app`：通过；
- `python -m unittest discover -v`：`Ran 234 tests ... OK`；
- 页面冒烟已通过：健康检查、登录、工作台、业务数据、上传批次、凭证上传、匹配审核和经营数据页面均无 500；
- 业务数据与经营数据 Excel 导出均返回正确的工作簿内容类型；
- 后续商城阶段不得删除或弱化现金返现链路的既有回归测试。

### 下一阶段

M1/v0.4.0 只建立商城领域骨架、迁移机制、数据库策略、权限与审计基础。在业务规则参数书面确认前，不直接开发商品页面或完整订单链路。
